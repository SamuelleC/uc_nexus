/** Matches `bg-green-950` (Tailwind default) — dashboard header & status bar */
export const BRAND_GREEN = "#052e16";

/** Default screen background (`bg-gray-100`) for status bar when leaving dashboard */
export const SCREEN_GRAY_BG = "#f3f4f6";
